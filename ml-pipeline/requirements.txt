# Requirements for ShadowSense Aurora — ML pipeline

# Core ML and Vector DB
chromadb>=0.5.0          # PersistentClient API
numpy>=1.24.0
pandas>=2.0.0

# Embeddings and NLP
# sentence-transformers >=2.6 adds normalize_embeddings to .encode()
sentence-transformers>=2.6.0
# CPU-only PyTorch (avoids pulling the full CUDA wheel in CI)
torch>=2.0.0
scikit-learn>=1.3.0

# Ollama (optional — used as a fallback embedding backend)
ollama>=0.1.0
requests>=2.31.0

# Utilities
python-dotenv>=1.0.0
pyyaml>=6.0

# Development / Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
black>=23.0.0
